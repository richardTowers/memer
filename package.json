{
  "name": "memer",
  "private": true,
  "version": "0.0.0",
  "description": "A meme generator to demonstrate alphagov/paas-s3-broker",
  "main": "index.ts",
  "scripts": {
    "start:dev": "ts-node index.ts",
    "start": "node index.js",
    "clean": "rm -r build",
    "build": "npm run clean && tsc && cp package-lock.json package.json build",
    "push": "npm run build && cf v3-create-app memer && cf v3-apply-manifest -f manifest.yml && cf v3-zdt-push memer -p build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.x"
  },
  "author": "Crown Copyright",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.16.1",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.4000"
  },
  "dependencies": {
    "aws-sdk": "^2.431.0",
    "express": "^4.16.4"
  }
}
